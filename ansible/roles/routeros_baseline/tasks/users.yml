---
# Service groups and group membership. Deliberately does NOT create users:
# creation (with random throwaway passwords + ssh-key upload) stays in the
# bootstrap/onboarding plays, so this role can never produce a passwordless
# account. It corrects group/policy drift on accounts that already exist.
# expand each short policy list to RouterOS's canonical stored form (granted
# then negated, universe order) so the string comparison is idempotent —
# see routeros_policy_universe in defaults
- name: Expand group policies to canonical form
  ansible.builtin.set_fact:
    service_groups_expanded: >-
      {{ (service_groups_expanded | default([])) + [item | combine({'policy': (
           (routeros_policy_universe | select('in', granted) | list)
           + (routeros_policy_universe | reject('in', granted)
              | map('regex_replace', '^', '!') | list)
         ) | join(',')})] }}
  vars:
    granted: "{{ item.policy.split(',') | map('trim') | list }}"
  loop: "{{ service_groups }}"
  loop_control:
    label: "{{ item.name }}"

- name: Ensure service groups exist with expected policies
  community.routeros.api_modify:
    path: user group
    data: "{{ service_groups_expanded | default([]) }}"

# read-only query; check_mode false so --check runs still get real data
# (community.routeros.api would otherwise be skipped, and the membership
# and audit logic below would choke on the skip placeholder)
- name: Fetch current users
  community.routeros.api:
    path: user
  register: baseline_users
  check_mode: false
  changed_when: false

# per-account, so a device missing one service account (not yet onboarded for
# that function) still gets the others corrected
- name: Correct group membership of service accounts
  community.routeros.api_modify:
    path: user
    data:
      - {name: "{{ item.name }}", group: "{{ item.group }}"}
  loop:
    - {name: oxidized, group: netops-backup-ro}
    - {name: oxidized-full, group: netops-backup-full}
    - {name: mktxp, group: mktxp_group}
    - {name: ansible, group: netops-ansible}
  when: item.name in (baseline_users.msg | map(attribute='name') | list)
